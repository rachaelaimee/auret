// TEMPORARY Firebase Firestore Security Rules - FOR TESTING ONLY
// Copy these rules to your Firebase Console -> Firestore Database -> Rules
// IMPORTANT: Replace with proper rules from firestore-rules.txt once testing is complete

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads and writes for authenticated users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
